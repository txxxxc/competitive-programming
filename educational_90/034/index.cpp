#include <bits/stdc++.h>
using namespace std;
#define debug(arg) std::cerr << #arg << ": " << (arg) << '\n'
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
using ll = long long;

int main() {
	int answer = 0;
	int N, K; cin >> N >> K;
	vector<int> list(N);
	rep (i, N) {
		cin >> list[i];
	}

	rep (i, N) {
		map<int, int> tmpList;
	}

	cout << answer << endl;
	return 0;
}

